name: Deploy Shiny App

on:
  push:
    branches: [main]
  schedule:
    - cron: '*/55 * * * *'  # every 15 minutes

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Setup R
      uses: r-lib/actions/setup-r@v2

    - name: Install rsconnect package
      run: Rscript -e "install.packages('rsconnect', repos='https://cloud.r-project.org')"

    - name: Deploy to shinyapps.io
      env:
        SHINYAPPS_NAME: ${{ secrets.SHINYAPPS_NAME }}
        SHINYAPPS_TOKEN: ${{ secrets.SHINYAPPS_TOKEN }}
        SHINYAPPS_SECRET: ${{ secrets.SHINYAPPS_SECRET }}
      run: |
        Rscript -e "rsconnect::setAccountInfo(name='${SHINYAPPS_NAME}', token='${SHINYAPPS_TOKEN}', secret='${SHINYAPPS_SECRET}')"
        Rscript -e "rsconnect::deployApp(appDir = 'tracker', appName = 'nwri-tracker')"
